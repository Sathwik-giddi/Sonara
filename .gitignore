.venv/
__pycache__/
*.pyc
.env
models/
*.onnx
*.bin
logs/
.uv/
.last_recording.wav

# Voice corpus: recordings of the owner's voice. Personal data, never publishes.
recordings/
.obsidian/


# Local agent memory (may contain project details — commit only after review)
.sonara-local/

# Private notes vault: personal session history and design memory.
# Lives inside the repo for convenience; must NEVER publish when Sonara goes open source.
Sonara Intelligence/
